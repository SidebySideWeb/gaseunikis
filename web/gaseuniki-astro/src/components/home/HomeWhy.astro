---
import PlaceholderImage from '../ui/PlaceholderImage.astro';
import { getImageUrl } from '../../lib/image';
import type { ImageDelivery } from '../../lib/image';
import type { Sponsor, WhyItem } from '../../lib/types';
import { stitchImages } from '../../lib/stitch-images';

interface Props {
  heading: string;
  items: WhyItem[];
  sponsorsHeading: string;
  sponsors: Sponsor[];
  image?: ImageDelivery;
  imageAlt?: string;
}

const {
  heading,
  items,
  sponsorsHeading,
  sponsors,
  image,
  imageAlt = 'Οφέλη συμμετοχής στον σύλλογο',
} = Astro.props;

const whyImage = image ?? { src: stitchImages.whyUs };
---

<section class="ribbon-bg py-section-padding">
  <div class="container-page grid items-start gap-16 md:grid-cols-2">
    <div>
      <h2 class="mb-8 font-display text-headline-lg text-on-surface">{heading}</h2>
      <ul class="space-y-6">
        {items.map((item) => (
          <li class="flex gap-4">
            <div class="flex h-10 w-10 shrink-0 items-center justify-center rounded-full border border-primary/20 bg-primary/10">
              <span class="material-symbols-outlined text-primary" aria-hidden="true">{item.icon ?? 'check'}</span>
            </div>
            <div>
              <h3 class="font-display text-headline-md text-on-surface">{item.title}</h3>
              <p class="text-on-surface-variant">{item.description}</p>
            </div>
          </li>
        ))}
      </ul>
    </div>

    <PlaceholderImage
      delivery={whyImage}
      alt={imageAlt}
      class="min-h-[320px] w-full rounded-3xl border border-primary/10 object-cover shadow-card"
    />
  </div>
</section>

{sponsors.length > 0 && (
  <section class="border-y border-primary/5 bg-surface-container py-16">
    <div class="container-page">
      <p class="mb-10 text-center font-display text-label-md uppercase tracking-widest text-primary">{sponsorsHeading}</p>
      <div class="flex flex-wrap items-center justify-center gap-10 md:gap-16 opacity-60 grayscale transition-all hover:opacity-100 hover:grayscale-0">
        {sponsors.map((sponsor) => {
          const logoUrl = getImageUrl(sponsor.logo, { width: 320, quality: 80, fit: 'max' });
          return (
            <div class="flex h-24 w-44 shrink-0 items-center justify-center md:h-28 md:w-52">
              {logoUrl ? (
                <img
                  src={logoUrl}
                  alt={sponsor.logo?.alt ?? sponsor.name}
                  class="max-h-full max-w-full object-contain"
                  loading="lazy"
                  decoding="async"
                />
              ) : (
                <span class="text-center font-display text-xl font-bold text-on-surface md:text-2xl">{sponsor.name}</span>
              )}
            </div>
          );
        })}
      </div>
    </div>
  </section>
)}
